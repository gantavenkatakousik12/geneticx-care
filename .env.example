# SMTP for enquiry-form delivery (Google Workspace).
# Every form submission emails the routed GeneticxCare mailbox (doc §13).
# Use a Google Workspace App Password, not the account password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=info@geneticxcare.com
SMTP_PASS=your-app-password-here
